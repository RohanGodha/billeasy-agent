# ---------------------------------------------------------------------------
# Core runtime — lean enough to build + run on Render's free tier (512 MB).
# RAG runs in BM25-only mode here (the HybridRetriever degrades gracefully when
# chromadb is absent). For full dense+BM25 hybrid locally, also install
# requirements-rag.txt.
# ---------------------------------------------------------------------------
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
httpx==0.27.2
aiosqlite==0.20.0
sse-starlette==2.1.3
tenacity==9.0.0
PyYAML==6.0.2
Faker==30.8.1
# numpy pinned <2.0 because databricks-sql-connector requires numpy<2.0.0.
# 1.26.4 satisfies rank-bm25 + our scoring (np.array/mean/std/linalg) too.
numpy==1.26.4
rank-bm25==0.2.2

# LLM providers (lazy-imported; safe to keep even when keys are unset)
anthropic==0.69.0
google-generativeai==0.8.3
groq==0.11.0

# Primary data source (Option A). Lazy-imported; only used when DATABRICKS_* set.
databricks-sql-connector==3.5.0

# Dev / CI
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.7.2
